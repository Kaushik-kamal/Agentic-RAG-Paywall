# Screenshots

Drop captures here with these exact names so the root README picks them up:

- `console.png`    — /console mid-stream, citation pills visible
- `protocol.png`   — /protocol after a completed handshake
- `retrieval.png`  — /console with the Retrieval tab open
- `dashboard.png`  — /dashboard with query volume and revenue

Capture at 1440x900 in dark mode, then repeat in light mode if you want a
side-by-side. Crop the browser chrome.
